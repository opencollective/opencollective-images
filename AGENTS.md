# opencollective-images

Image upload, processing, and optimization. Stores files on S3 (production) or MinIO (local). No TypeScript check script.

## Quality

From this repo: `npm run lint:quiet`, `npm run prettier:check` (fix: `prettier:write`). Tests: `npm run test`. Schema: `npm run graphql:update` (API must be running).
