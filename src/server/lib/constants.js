export const MAX_AVATAR_HEIGHT = 512;

// Limits for the image proxy, where both the source and the dimensions are user provided
export const MAX_PROXY_IMAGE_BYTES = 10 * 1024 * 1024;
export const MAX_PROXY_IMAGE_DIMENSION = 3000;
export const PROXY_FETCH_TIMEOUT = 10 * 1000;
